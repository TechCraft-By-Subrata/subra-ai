# Subra AI public configuration

The schema v1 JSON files in `config/v1/` are draft inputs for the planned Spaces experience. The current mobile app does not fetch them yet. A future app release will fetch the schema folder it supports; compatible content updates stay in that folder.

# Subra AI public configuration

The schema v1 JSON files in `config/v1/` are draft inputs for the Spaces experience. The app source now bundles a validated v1 snapshot and can check for a newer published config at the matching GitHub Pages URL. These files are still drafts and have not been published through Pages. Compatible content updates stay in this folder; a breaking schema requires a new folder and app release.
